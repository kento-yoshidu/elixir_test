version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18.0.0
        - nvm use 18.0.0
        - npm ci
    build:
      commands:
        - node -v
        - npm run build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18.0.0
        - nvm use 18.0.0
        - node -v
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
